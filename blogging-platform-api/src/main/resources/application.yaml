spring:
  application:
    name: blogging-platform-api
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/blogging_platform
    username: postgres
    password: postgres